import os

# config_dir = "./configs/"  # Change it according to the directory of the platform on which the code is being run
# dataset_dir = "./data/"
# save_location = "./plots/"
models_dir = "./models"

# os.makedirs(config_dir, exist_ok=True)
# os.makedirs(dataset_dir, exist_ok=True)
# os.makedirs(save_location, exist_ok=True)
os.makedirs(models_dir, exist_ok=True)

